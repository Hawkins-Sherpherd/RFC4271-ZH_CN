# KEEPALIVE 报文格式

BGP 不使用任何基于 TCP 的保活机制来确定对等体是否可达，而是使用一种在对等体之间交换 KEEPALIVE 报文的通常足以使保持计时器不超时的方式来保活。一个合理的 KEEPALIVE 报文的最大发送间隔时间应该是保持时间间隔的三分之一。KEEPALIVE 报文**不应该**以高于每个一秒的频率发送。一个实现**可以**根据保持时间间隔的大小调整发送 KEEPALIVE 报文的频率。

如果协商的保持时间间隔为0，则**必须不**发送周期性的 KEEPALIVE 报文。

一个 KEEPALIVE 报文只由报文头组成，所以其长度为19个八位元。